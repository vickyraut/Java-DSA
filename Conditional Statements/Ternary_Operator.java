public class Ternary_Operator {
    public static void main(String[] args) {
        int b = 32;

        // Ternary Operator
        String odd_even = (b % 2 == 0) ? "EVEN" : "ODD";
        System.out.println(odd_even);
    }
}
